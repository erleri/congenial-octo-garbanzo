# LatamFX 향후 운영 개선 계획

## 목적

LatamFX가 시제품에서 실제 운영 시스템으로 발전하면서 추가로 점검해야 할 보안, 데이터, 배포, 권한 및 장애 대응 과제를 정리한다.

현재 운영 구조는 다음을 기준으로 한다.

- GitHub: 앱 코드와 자동화 설정
- GitHub Actions: 환율 수집, 계산, 검증 및 Supabase 적재
- Supabase: 시장 환율, 계획 환율, 로그인과 권한
- Netlify: 프론트엔드 배포
- IndexedDB와 JSON: 장애 시 복구 경로

이 문서의 과제는 한 번에 모두 적용하지 않는다. 운영 안정성을 해치지 않도록 기준점을 보존하고 별도 브랜치에서 단계적으로 검증한다.

---

## 우선순위 1: 공개 데이터와 비밀정보 정리

### 1. 메일 수신자 목록 비공개화

현재 공개 GitHub 저장소에 `data/mailing_list.json`이 포함되어 있다. 실제 이메일 주소가 들어 있다면 개인정보가 외부에 노출될 수 있다.

개선 방향:

- 메일 수신자 목록을 Supabase의 비공개 테이블 또는 GitHub Secret 등으로 이전한다.
- 수신자 조회·수정은 인증된 관리자만 가능하게 한다.
- Git 기록에 포함된 기존 이메일의 제거 필요성을 검토한다.
- 운영 화면에서 전체 수신자 정보를 익명 사용자에게 노출하지 않는다.

완료 기준:

- 공개 저장소와 배포 파일에서 실제 이메일 주소를 찾을 수 없다.
- 관리자만 수신자 목록을 조회하고 변경할 수 있다.
- 이메일 발송 작업은 신규 저장소에서 수신자를 정상적으로 읽는다.

### 2. 원본 시트 데이터 공개 범위 축소

`public/raw-sheets.json`은 약 36.8MB이며 공개 웹사이트에서 접근 가능한 배포 파일이다. 원본 업무 자료가 포함되어 있다면 필요 이상의 데이터 공개가 될 수 있다.

개선 방향:

- 원본 시트가 운영 화면에 반드시 필요한지 재검토한다.
- 불필요하면 Netlify 배포 대상에서 제외한다.
- 필요하면 관리자 인증 후 서버 측 API로 제한된 범위만 제공한다.
- CSV 내려받기와 원본 데이터 조회 기능도 관리자 권한으로 제한한다.

완료 기준:

- 익명 사용자가 원본 시트 파일에 직접 접근할 수 없다.
- 운영 화면은 필요한 집계 데이터만 사용한다.
- 원본 조회가 필요한 경우 접근 기록과 권한 기준이 있다.

### 3. 브라우저용 환율 API 키 제거

`VITE_` 환경변수는 브라우저 번들에 포함된다. 따라서 `VITE_EXCHANGERATE_API_KEY`와 `VITE_ALPHA_VANTAGE_API_KEY`를 운영 프론트엔드에서 사용하면 비밀키로 보호할 수 없다.

현재 API 호출 실패 시 API 키가 포함된 URL이 외부 CORS 프록시를 거칠 가능성도 있다.

개선 방향:

- 운영 브라우저에서는 외부 환율 API를 직접 호출하지 않는다.
- 비밀 API 키는 GitHub Actions 또는 서버 측 함수에서만 사용한다.
- 공용 CORS 프록시 경로를 운영 코드에서 제거한다.
- 화면의 `데이터 갱신`은 외부 API 재수집이 아니라 Supabase 최신 데이터 다시 읽기로 변경한다.

완료 기준:

- 운영 JavaScript 번들과 네트워크 요청에 환율 API 키가 나타나지 않는다.
- 사용자가 갱신 버튼을 눌러도 외부 유료 API를 직접 호출하지 않는다.
- 데이터 수집 권한은 GitHub Actions 등 서버 측 실행 환경에만 있다.

---

## 우선순위 2: 데이터 갱신과 배포 분리

### 4. JSON 자동 커밋과 매일 Netlify 재배포 중단

현재 GitHub Actions는 `public/data.json`과 일부 보조 데이터를 `main`에 자동 커밋한다. 이로 인해 앱 코드가 바뀌지 않아도 Netlify가 매일 재배포될 수 있다.

개선 방향:

- 안정화 기간 동안 Supabase와 JSON 결과를 계속 비교한다.
- 안정화 완료 후 `main` 브랜치의 JSON 자동 커밋을 중단한다.
- GitHub Actions는 다음 작업만 수행한다.
  - 환율 수집과 계산
  - Supabase 적재와 검증
  - 이메일 생성과 발송
  - 복구용 JSON 아티팩트 또는 별도 백업 저장
- Netlify는 앱 코드나 배포 설정이 변경될 때만 배포한다.

완료 기준:

- 일일 데이터 갱신만으로 Netlify 배포가 발생하지 않는다.
- Supabase에는 최신 환율이 정상 반영된다.
- 복구용 데이터는 GitHub `main` 이외의 안전한 위치에 보관된다.
- 필요 시 코드 변경 없이 데이터 갱신과 복구가 가능하다.

### 5. 데이터 생성과 이메일 작업 분리

현재 한 워크플로 안에서 데이터 수집, Supabase 적재, Git 커밋, 차트 생성과 이메일 발송이 연속 수행된다. 한 단계의 장애가 전체 작업에 영향을 줄 수 있다.

개선 방향:

- 데이터 수집·검증 작업과 이메일 발송 작업을 분리한다.
- 이메일은 `load_status=ready`인 검증 완료 데이터만 사용한다.
- 데이터 생성 실패 시 이전 정상 데이터로 이메일을 보낼지, 발송을 중단할지 기준을 정한다.
- 각 작업의 성공과 실패를 별도로 확인할 수 있게 한다.

완료 기준:

- 잘못된 데이터가 이메일로 발송되지 않는다.
- 데이터 적재 실패와 이메일 발송 실패를 구분할 수 있다.
- 재실행 시 중복 이메일 발송을 방지한다.

---

## 우선순위 3: 관리 기능과 권한 강화

### 6. 관리 화면 접근 제한

현재 관리 탭은 익명 사용자도 열 수 있다. 일부 기능은 로컬 전용이지만 데이터 상태와 원본 자료 접근 범위를 더 명확히 분리할 필요가 있다.

개선 방향:

- 일반 사용자 화면과 관리자 화면을 분리한다.
- 관리자 화면은 Supabase 인증과 관리자 테이블을 통해 접근을 제한한다.
- Excel 업로드, 원본 시트 조회, CSV 내려받기, 메일 수신자 관리는 관리자에게만 허용한다.
- 운영에서 사용할 수 없는 기능은 숨기거나 `로컬 미리보기 전용`으로 명확히 표시한다.

완료 기준:

- 익명 또는 일반 사용자가 관리자 기능과 원본 자료에 접근할 수 없다.
- 권한 검사는 화면 표시뿐 아니라 데이터베이스와 서버 측에서도 수행된다.
- 관리자 로그인과 로그아웃, 권한 회수 절차가 검증된다.

### 7. 계획 환율 저장 버전 관리

현재 계획 환율 저장 시 통화와 유형별 행이 계속 추가된다. 이력은 남지만 한 번의 저장 작업을 하나의 버전으로 식별하기 어렵고 장기적으로 중복 행이 증가한다.

개선 방향:

- 한 번의 저장을 나타내는 `revision_id` 또는 버전 테이블을 추가한다.
- 저장자, 저장 시각, 대상 기간과 변경 사유를 버전 단위로 기록한다.
- 최신 버전 조회와 이전 버전 복구를 명확히 분리한다.
- 동시 수정 충돌을 감지하는 방식을 검토한다.

완료 기준:

- 한 번의 저장 작업에 포함된 값을 하나의 버전으로 조회할 수 있다.
- 누가 언제 무엇을 변경했는지 확인할 수 있다.
- 이전 버전 비교와 복구가 가능하다.

### 8. Supabase 권한 정책 재검증

RLS가 적용되어 있지만 실제 관리자·비관리자·익명 사용자의 동작을 운영 환경에서 회귀 테스트해야 한다.

개선 방향:

- 익명 사용자의 시장 환율·계획 환율 읽기 범위를 확인한다.
- 비관리자 로그인 사용자가 계획 환율을 수정할 수 없는지 확인한다.
- 활성 관리자만 저장할 수 있는지 확인한다.
- 인증 이메일 변경, 관리자 비활성화와 세션 만료 시 권한 동작을 확인한다.
- `SECURITY DEFINER` 함수의 위치와 실행 권한을 정기 검토한다.

완료 기준:

- 익명, 일반 사용자, 관리자별 권한 테스트가 자동화되거나 문서화된다.
- 관리자 해제 후 기존 계정이 더 이상 저장할 수 없다.
- 공개 함수와 테이블의 권한이 최소 범위로 설정된다.

---

## 우선순위 4: 운영 관측과 장애 대응

### 9. 자동 장애 감지와 담당자 알림

현재 화면의 폴백과 적재 실패 상태는 있지만 담당자가 장애를 즉시 알 수 있는 체계가 부족하다.

감지 대상:

- 정해진 시각까지 일일 데이터 갱신이 완료되지 않음
- 일별 또는 월별 행 수가 비정상적으로 감소함
- 특정 통화나 날짜가 누락됨
- Supabase 적재 또는 검증 실패
- 이메일 생성·발송 실패
- 데이터 기준일이 일정 기간 이상 오래됨

개선 방향:

- GitHub Actions 실패 알림과 별도의 운영 알림을 구성한다.
- `fx_dataset_state`를 기준으로 데이터 신선도와 적재 상태를 점검한다.
- 장애 알림에 실패 단계, 기준일, 마지막 정상 갱신 시각과 복구 방법을 포함한다.

완료 기준:

- 담당자가 화면을 직접 열지 않아도 주요 장애를 인지한다.
- 알림만 보고 실패 단계와 우선 조치를 판단할 수 있다.
- 장애 종료 후 정상 복구 여부도 확인할 수 있다.

### 10. 복구 절차와 정기 훈련

폴백 기능이 존재해도 실제 장애 상황에서 작동하는지 정기적으로 확인해야 한다.

개선 방향:

- `VITE_FX_DATA_SOURCE=json` 전환 절차를 문서화한다.
- Supabase 장애, 느린 응답, 오프라인과 잘못된 데이터 버전을 테스트한다.
- IndexedDB 캐시와 JSON 복구 경로의 데이터 기준일을 화면에 표시한다.
- 복구 후 Supabase 운영 경로로 돌아오는 절차도 검증한다.

완료 기준:

- 운영자가 코드 변경 없이 데이터 경로를 전환할 수 있다.
- 캐시 또는 JSON 사용 중임을 사용자가 명확히 알 수 있다.
- 복구 훈련 결과와 소요 시간이 기록된다.

---

## 우선순위 5: 자동화 품질과 유지보수

### 11. GitHub Actions 권한과 재현성 개선

개선 방향:

- 의존성 설치는 가능한 경우 `npm install` 대신 `npm ci`를 사용한다.
- 각 워크플로의 GitHub 권한을 최소화한다.
- `contents: write`는 실제 커밋이 필요한 작업에만 부여한다.
- 데이터 수집, 검증, 이메일과 배포 작업을 역할별로 분리한다.
- 실패한 데이터가 정상 데이터 상태를 덮어쓰지 않도록 유지한다.

완료 기준:

- 동일한 `package-lock.json`으로 재현 가능한 실행 결과가 나온다.
- 불필요한 저장소 쓰기 권한이 제거된다.
- 각 작업의 책임과 재실행 범위가 명확하다.

### 12. 운영 문서 최신화

`README.md`와 `OPERATIONS_TODO.md`에 과거 JSON 중심 구조와 오래된 검증 결과가 일부 남아 있다.

개선 방향:

- 현재 운영 구조를 Supabase 중심으로 수정한다.
- 완료된 초기 적재·검증 항목을 최신 상태로 갱신한다.
- 매일 Netlify가 배포되는 것처럼 보이는 설명을 수정한다.
- 관리자 권한, 장애 대응, 데이터 복구와 배포 절차를 분리해 문서화한다.

완료 기준:

- 신규 담당자가 문서만 보고 현재 데이터 흐름을 설명할 수 있다.
- 운영 절차가 실제 GitHub Actions와 Netlify 설정과 일치한다.
- 오래된 커밋 번호와 기준일이 운영 문서에 남아 있지 않다.

---

## 권장 실행 순서

1. 공개 이메일과 원본 시트 노출 여부를 먼저 정리한다.
2. 브라우저의 비밀 API 키와 공용 CORS 프록시 사용을 제거한다.
3. Supabase 안정화 기간을 거친 뒤 JSON 자동 커밋을 중단한다.
4. 데이터 수집과 이메일 발송 작업을 분리한다.
5. 관리 화면과 계획 환율 권한을 강화한다.
6. 장애 감지, 알림과 복구 훈련을 운영 체계에 포함한다.
7. 계획 환율 버전 관리와 자동화 품질을 개선한다.
8. README와 운영 문서를 최종 구조에 맞게 갱신한다.

## 공통 작업 원칙

- 모든 변경은 별도 Git 브랜치에서 수행한다.
- 데이터 구조 변경 전 현재 운영 기준점을 태그 또는 커밋으로 보존한다.
- 기존 경로를 즉시 삭제하지 않고 신규·기존 구조를 병행 검증한다.
- 보안, 데이터 일치와 장애 복구 테스트를 통과한 뒤 운영에 반영한다.
- 운영 전환 후에도 일정 기간 롤백 경로를 유지한다.
